name: CI/CD Pipeline
# TEST TO RETRIGGER
on:
  push:
    branches:
      - main
      - feature/*
  pull_request:
    branches:
      - main

jobs:
  # 1. Run Tests (no changes needed here)
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4 # Updated to latest v4

      - name: Set up Node.js
        uses: actions/setup-node@v4 # Updated to latest v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test || echo "⚠️ No tests yet - skipping"

  # 2. Build, Scan, & Push Docker Image (Updated logic)
  build-scan-and-push:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4 # Updated to latest v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3 # Updated to latest v3

      # ----------------------------------------------------------------------
      # DOCKER LOGIN
      # ----------------------------------------------------------------------
      - name: Log in to Docker Hub
        uses: docker/login-action@v3 # Updated to latest v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Define Image Tag
        id: image_tag
        run: echo "IMAGE_TAG=${{ secrets.DOCKER_USERNAME }}/electromart:latest" >> $GITHUB_OUTPUT

      # ----------------------------------------------------------------------
      # BUILD IMAGE (DO NOT PUSH YET)
      # ----------------------------------------------------------------------
      - name: Build Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false # We use 'push: false' here so the image is available for the next step (Trivy)
          tags: ${{ steps.meta.outputs.tags }}
          # ADD THESE LINES TO FORCE A FRESH BUILD
          no-cache: true

     # ----------------------------------------------------------------------
      # SECURITY SCAN (Trivy)
      # ----------------------------------------------------------------------
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        timeout-minutes: 5
        with:
          image-ref: ${{ steps.image_tag.outputs.IMAGE_TAG }}
          format: 'table'
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'
          exit-code: '1' # Set to '1' to enforce security and fail the build

      # ----------------------------------------------------------------------
      # PUSH IMAGE (Only if the Scan Passes)
      # ----------------------------------------------------------------------
      - name: Push Docker Image to DockerHub
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true # Push the now-verified image
          tags: ${{ steps.image_tag.outputs.IMAGE_TAG }}